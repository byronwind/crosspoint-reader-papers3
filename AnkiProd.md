
# AnkiEInk for M5Stack PaperS3 产品需求文档

**完整版 PRD V1.1（修订版）**

---

## 目录
1. [文档概述](#1-文档概述)
2. [产品愿景与目标](#2-产品愿景与目标)
3. [硬件规格与约束](#3-硬件规格与约束)
4. [系统架构设计](#4-系统架构设计)
5. [功能需求详述](#5-功能需求详述)
6. [非功能需求](#6-非功能需求)
7. [用户界面规范](#7-用户界面规范)
8. [数据模型设计](#8-数据模型设计)
9. [API接口规范](#9-api接口规范)
10. [测试策略](#10-测试策略)
11. [开发计划与里程碑](#11-开发计划与里程碑)
12. [版本迭代规划](#12-版本迭代规划)

---

## 1. 文档概述

### 1.1 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | AnkiEInk（墨水屏Anki复习助手） |
| 文档版本 | V1.1 |
| 文档状态 | 定稿 |
| 创建日期 | 2026-08-12 |
| 修订日期 | 2026-08-12 |
| 目标硬件 | M5Stack PaperS3 |
| 固件代号 | AnkiEInk-FW |

### 1.2 术语表

| 术语 | 说明 |
|------|------|
| FSRS | Free Spaced Repetition Scheduler，自由间隔重复调度算法 |
| APKG | Anki Package，Anki牌组文件格式 |
| E-Ink | 电子墨水屏，本设备采用4.7英寸灰阶屏 |
| IT8951 | E-Ink显示驱动芯片 |
| AXP2101 | 电源管理芯片（PMU） |
| NVS | Non-Volatile Storage，ESP32非易失性存储 |
| WAL | Write-Ahead Logging，SQLite写入前日志模式 |
| STA/AP | WiFi工作模式：终端模式/热点模式 |
| GC16 | E-Ink 16 灰阶全局刷新模式，清晰度高，用于菜单、卡片正文、统计图表 |
| A2 | E-Ink 快速二值刷新模式，响应快但残影明显，用于评分按钮按下、局部进度更新 |
| DU4 | E-Ink 4 灰阶差分更新模式，介于 A2 与 GC16 之间，用于卡片翻页门帘动画 |
| GL16 | E-Ink 灰度快速刷新模式（IT8951 特有），替代 DU4 作为过渡场景的折中方案 |
| 门帘刷新 | 卡片翻转时的 E-Ink 视觉动效，从中间或一侧扫描式过渡以掩盖残影 |
| New | FSRS 卡片状态：未复习过的新卡片 |
| Learning | FSRS 卡片状态：初始学习阶段（短间隔递进） |
| Review | FSRS 卡片状态：已学习完成，进入长间隔复习循环 |
| Relearning | FSRS 卡片状态：遗忘后重新学习（lapse 触发） |
| 间隔重复 (Spaced Repetition) | 根据记忆强度动态调整下次复习时间的学习方法 |
| 期望记忆率 (Request Retention) | FSRS 参数 [0.70, 0.99]，调度器目标的用户长期记忆保持率 |
| 稳定性 (Stability) | FSRS 核心状态量，表征记忆半衰期（天） |
| 难度 (Difficulty) | FSRS 核心状态量，表征卡片固有难度 [1, 10] |

---

## 2. 产品愿景与目标

### 2.0 核心定位 (Elevator Pitch)
基于 M5Stack PaperS3 墨水屏的**离线专用 Anki 复习终端**，以 FSRS V5 精准调度、物理隔离手机干扰、超长续航为核心卖点，为考研/医考/语言学习者提供极致专注的记忆体验。

### 2.1 产品愿景
打造一款**真正离线、极致续航、护眼专注**的Anki复习硬件终端，让学习者摆脱手机的干扰，在墨水屏的沉浸体验中完成高效记忆。

### 2.2 目标用户画像

| 用户角色 | 特征 | 核心需求 |
|----------|------|----------|
| 考研学生 | 每天长时间复习，反感手机通知干扰 | 专注模式、长续航、精准的间隔调度 |
| 医学生 | 牌组庞大（万张级），需要精细统计 | 大数据量支持、详细复习统计 |
| 语言学习者 | 多设备间切换使用 | 同步便捷、APKG兼容性高 |

### 2.3 核心价值主张
- **断舍离**：物理隔离手机通知，单功能设备。
- **算法精准**：FSRS V5算法，优于传统SM-2。
- **超长续航**：目标待机30天，连续复习15小时。
- **开放兼容**：完全兼容Anki生态，APKG直接导入。

### 2.4 成功指标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 系统启动时间 | < 3秒 | 从冷启动到主菜单可交互 |
| 卡片翻页延迟 | < 400ms | 触摸到屏幕更新完成 |
| 待机功耗 | < 2mA | 深度休眠模式电流测量 |
| 连续复习续航 | > 15小时 | 每分钟1张卡片的节奏 |
| APKG兼容率 | > 95% | 市面常见公开牌组 |
| WiFi配网成功率 | > 98% | 首次配网成功率 |
| FSRS运算时间 | < 50ms | 单次卡片评分计算 |

### 2.5 竞品对比矩阵

| 对比维度 | 智能手机 Anki | 电子阅读器（Kindle/Onyx） | 专用记忆硬件（AnkiEInk） |
|----------|---------------|--------------------------|--------------------------|
| 护眼程度 | 差：LCD/OLED蓝光伤眼，长时间使用疲劳 | 好：E-Ink墨水屏，自然光反射 | 好：E-Ink墨水屏，16灰阶优化，无蓝光 |
| 专注干扰度 | 差：通知、社交APP、游戏随时打断 | 中：无通知但可切换书籍/浏览器 | 好：物理单功能设备，仅可复习Anki |
| 续航时长 | 差：重度使用3-5小时，待机1-2天 | 中：阅读可2-4周，需刷新页面 | 好：目标待机30天，连续复习15小时+ |
| FSRS 算法支持 | 中：需安装插件，AnkiMobile不支持 | 差：无原生Anki支持，第三方适配困难 | 好：原生内置FSRS V5，硬件FPU加速 |
| Anki 生态兼容 | 好：官方客户端，完全兼容APKG | 差：Kindle封闭，Onyx需装Android版Anki | 好：直接导入APKG，95%+牌组兼容 |
| 便携性 | 中：手机尺寸通用，但已日常携带 | 中：6-10寸设备，单手握持稍大 | 好：4.7寸口袋尺寸，专用设备随身携带 |
| 价格估算 | 中：手机已有或¥2000+新购 | 中：¥1000-2500阅读器 | 低：预估¥300-500硬件成本 |

### 2.6 典型用户使用场景

- **场景 1：考研党早自习离线复习**
  - 触发：早上7点到图书馆，手机放在宿舍，携带AnkiEInk设备。
  - 操作：长按电源键开机→主菜单选择"开始复习"→选择"考研政治"牌组→逐张复习，按评分按钮（重来/困难/良好/简单）→完成后显示今日统计→长按电源键手动休眠。
  - 预期结果：冷启动<3秒进入主菜单，卡片翻页<400ms，全程无WiFi无干扰，复习进度实时保存，完成页面显示复习张数、平均用时、各评分比例。

- **场景 2：医学生万张牌组导入**
  - 触发：获取到学长分享的"解剖学20000张.apkg"文件，需导入设备。
  - 操作：设备端进入"Web管理"模式→手机连接AnkiEInk热点→浏览器打开配网页面→切换到牌组管理→点击上传APKG→选择文件（分片上传）→WebSocket实时显示导入进度→导入完成后查看牌组详情。
  - 预期结果：导入速度>100张/秒，进度条实时更新，导入完成自动校验卡片数，支持媒体文件存SD卡，导入失败给出明确错误信息。

- **场景 3：通勤图书馆Web后台配网**
  - 触发：第一次在图书馆使用，需连接图书馆WiFi以便夜间自动同步时间。
  - 操作：设备首次开机自动进入AP模式→手机搜索SSID"AnkiEInk-XXXX"并连接→浏览器访问192.168.4.1→进入WiFi设置页→扫描WiFi列表→选择图书馆WiFi输入密码→连接成功→设备自动关闭AP切换STA模式→提示配网成功。
  - 预期结果：配网成功率>98%，WiFi列表显示信号强度，密码错误明确提示，最多保存5个WiFi配置，后续自动连接信号最强的已保存网络。

- **场景 4：夜间护眼复习**
  - 触发：晚上11点宿舍熄灯，想复习30分钟不打扰室友。
  - 操作：从休眠状态触摸屏幕唤醒（无需按电源键）→继续上次复习会话→使用GC16模式显示文字（无背光，不刺眼）→每5页自动全刷清除残影→30分钟无操作自动进入深度休眠。
  - 预期结果：唤醒<1秒恢复上次界面，无背光纯墨水显示不刺眼，残影控制良好，自动休眠前保存当前队列位置，下次唤醒无缝继续。

- **场景 5：低电量自动休眠唤醒**
  - 触发：复习过程中电池电量降至5%以下。
  - 操作：（系统自动触发）状态栏电池图标开始闪烁提醒→电量<5%时弹出"低电量即将休眠"提示框→3秒倒计时后自动保存所有数据→ESP32进入Deep Sleep+IT8951进入Standby→用户充电后按电源键唤醒。
  - 预期结果：数据在提示框出现前已完成持久化（WAL模式），休眠电流<500μA，唤醒后完整恢复休眠前的复习界面和队列位置，无任何数据丢失。

### 2.7 核心功能边界

**IN SCOPE（V1.0 必须实现）：**
- FSRS V5 调度引擎与4档卡片评分（重来/困难/良好/简单），单次计算<50ms。
- APKG 牌组导入导出（ZIP流式解析+媒体文件SD卡存储）与完全离线复习。
- E-Ink 专用 UI 交互（状态栏/主菜单/复习界面/设置页），A2/GC16/DU4波形策略。
- Web 管理后台：仪表盘、牌组管理、FSRS参数设置、复习统计、OTA固件升级。
- WiFi 配网（AP+STA双模式）、NTP定时时间同步、自动休眠+低电量休眠+触摸唤醒功耗管理。

**OUT OF SCOPE（V1.0 明确不做）：**
- 不支持实时在线云同步（仅NTP时间同步为唯一联网操作，牌组同步通过APKG文件导入导出）。
- 不支持音频/视频播放（媒体文件仅处理图片，音频标签降级为纯文本占位符）。
- 不支持多用户账户体系（单设备单用户，无登录/注册/权限管理）。
- 不支持卡片编辑（仅支持导入/删除/查看，卡片内容编辑需在PC端Anki完成后重新导出）。
- 不支持彩色图像显示（4.7寸灰阶屏限制，所有图片通过抖动算法转换为16灰阶）。

---

## 3. 硬件规格与约束

### 3.1 目标硬件参数

| 组件 | 规格 | 固件影响 |
|------|------|----------|
| MCU | ESP32-S3R8, 双核240MHz | 双核任务分配，FPU启用 |
| PSRAM | 8MB Octal PSRAM | HTML渲染缓冲、APKG解析 |
| Flash | 16MB | 分区表定制，固件+Web资源 |
| E-Ink | 4.7", 960×540, 16灰阶 | IT8951驱动，波形优化 |
| 触摸 | GT911 电容式 | I2C中断驱动 |
| PMU | AXP2101 | 电池管理、休眠控制 |
| SD卡 | MicroSD 插槽 | 备份存储、OTA固件 |

### 3.2 Flash分区表设计

```
# partitions.csv
# Name,       Type,   SubType,  Offset,    Size,     Flags
nvs,          data,   nvs,      0x9000,    0x6000,
otadata,      data,   ota,      0xf000,    0x2000,
phy_init,     data,   phy,      0x11000,   0x1000,
factory,      app,    factory,  0x20000,   0x3C0000,  # 3.75MB 主固件
web_res,      data,   fat,      0x3E0000,  0x100000,  # 1MB Web前端资源
deck_db,      data,   spiffs,   0x4E0000,  0x600000,  # 6MB 牌组数据库
ota_0,        app,    ota_0,    0xAE0000,  0x3C0000,  # OTA分区A
ota_1,        app,    ota_1,    0xEA0000,  0x3C0000,  # OTA分区B
nvskey,       data,   nvs_keys, 0x1260000, 0x4000,
```

**存储分工说明**
deck_db 分区（6MB SPIFFS）为 SQLite 主数据库热存储，存放卡片元数据、FSRS 状态与复习日志。
媒体文件（图片/音频）、大容量 APKG 临时包、OTA 固件、数据库备份归档全部存放于外置 MicroSD 卡，目录结构详见 3.4 节。
数据库单文件超过 deck_db 分区 80% 时，提示用户清理或迁移媒体至 SD 卡。

**总计**: 约16MB Flash完全利用

### 3.3 硬件约束与对策

| 约束 | 影响 | 对策 |
|------|------|------|
| 内存有限（8MB PSRAM） | 大牌组解析困难 | 流式解析APKG，分批导入 |
| E-Ink刷新慢 | 交互延迟感 | A2模式快速响应，GC16模式清晰显示 |
| E-Ink残影 | 旧内容残留 | 每5页全刷一次，关键页面GC16 |
| WiFi功耗高 | 续航缩短 | 定时开关WiFi，Web15分钟无操作关闭 |
| 无实体键盘 | 输入不便 | Web端配置为主，设备端仅评分按钮 |
| 灰阶限制 | 图片显示效果差 | 专用抖动算法，图片预处理 |

### 3.4 SD 卡分层存储策略

**3.4.1 前置条件**
- 支持 MicroSD（TF）卡，最大 32GB（SDHC，FAT32 文件系统，4K 簇对齐）。
- SD 卡不存在时降级运行：仅允许纯文本卡片（无媒体）导入、禁用 OTA URL 下载、禁用自动备份。
- 首次插入自动创建目录结构。

**3.4.2 目录结构规范**
```
/                          # SD 卡根
├── AnkiEInk/              # 应用工作目录（避免与其他应用冲突）
│   ├── apkg_cache/        # APKG 临时文件（导入完成后自动删除）
│   ├── media/             # 媒体资源库（图片/音频，按 MD5 首 2 位分桶）
│   │   ├── 00/
│   │   ├── 01/
│   │   └── ... /
│   ├── backup/            # 数据库每日自动备份（保留最近 7 份）
│   │   └── deck_YYYYMMDD.db.gz
│   ├── fonts/             # 第三方用户字体（TTF/OTF，详见 7.3.2 节）
│   ├── ota/               # OTA 固件包缓存（factory.bin / ota.bin）
│   └── logs/              # 系统运行日志（按天切割，保留 30 天）
└── System Volume Information /  # 忽略
```

**3.4.3 存储分工矩阵**

| 数据类别 | 主存储位置 | 备份位置 | 最大限额 | 自动清理策略 |
|----------|-----------|---------|---------|-------------|
| SQLite 数据库 | Flash: deck_db 分区 | SD: /AnkiEInk/backup/ | 6MB（分区 80% 预警） | 备份保留 7 天 |
| APKG 临时包 | SD: apkg_cache | - | 500MB | 导入完成立即删除 |
| 媒体资源 | SD: media | - | 按 SD 容量 | 无（手动清理） |
| 复习日志 90 天内 | Flash: deck_db | SD: backup | 含于数据库 | 90 天后自动归档 SD |
| 第三方字体 | SD: fonts | - | ≤ 512MB | 无（手动管理） |
| OTA 固件 | SD: ota | - | ≤ 16MB | 更新后删除旧版本 |
| 系统日志 | SD: logs | - | ≤ 100MB | 按天滚动，保留 30 天 |

---

## 4. 系统架构设计

### 4.1 分层架构图

```
┌─────────────────────────────────────────────┐
│               应用层 (Application)            │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ 复习UI   │  │ Web后台  │  │ 设置管理   │ │
│  │ ReviewUI │  │ WebAdmin │  │ Settings   │ │
│  └────┬─────┘  └────┬─────┘  └─────┬──────┘ │
├───────┼──────────────┼─────────────┼─────────┤
│       │    业务逻辑层 (Business Logic)       │
│  ┌────┴──────────────┴─────────────┴──────┐ │
│  │          FSRS调度引擎                  │ │
│  │  ┌──────────┐  ┌──────────────────┐   │ │
│  │  │ 调度器   │  │  参数优化器      │   │ │
│  │  │Scheduler │  │  Optimizer       │   │ │
│  │  └──────────┘  └──────────────────┘   │ │
│  └────────────────┬──────────────────────┘ │
│  ┌────────────────┴──────────────────────┐ │
│  │         牌组管理模块                  │ │
│  │  ┌──────────┐  ┌──────────────────┐   │ │
│  │  │ APKG解析 │  │  卡片渲染引擎    │   │ │
│  │  └──────────┘  └──────────────────┘   │ │
│  └──────────────────────────────────────┘ │
├───────────────────────────────────────────┤
│           服务层 (Services)                │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐│
│  │HTTP  │ │WebSoc│ │WiFi  │ │NTP时间   ││
│  │Server│ │ket   │ │Mgr  │ │同步      ││
│  └──┬───┘ └──┬───┘ └──┬───┘ └────┬─────┘│
├─────┼────────┼────────┼──────────┼───────┤
│     │  抽象层 (HAL)         │              │
│  ┌──┴────────┴────────┴──────────┴──────┐ │
│  │  ┌──────┐ ┌──────┐ ┌──────┐         │ │
│  │  │显示  │ │触摸  │ │电源  │         │ │
│  │  │IT8951│ │GT911 │ │AXP   │         │ │
│  │  └──────┘ └──────┘ └──────┘         │ │
│  └──────────────────────────────────────┘ │
├───────────────────────────────────────────┤
│        平台层 (ESP-IDF / FreeRTOS)         │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐│
│  │WiFi  │ │SPIFFS│ │SQLite│ │FreeRTOS  ││
│  │Stack │ │      │ │      │ │Scheduler ││
│  └──────┘ └──────┘ └──────┘ └──────────┘│
└───────────────────────────────────────────┘
```

### 4.2 核心任务分配

| 任务名称 | 绑定核心 | 优先级 | 栈大小 | 说明 |
|----------|----------|--------|--------|------|
| `httpd_task` | Core 0 | 5 | 8192 | HTTP服务器 |
| `ws_server_task` | Core 0 | 4 | 4096 | WebSocket服务 |
| `wifi_event_task` | Core 0 | 6 | 4096 | WiFi事件处理 |
| `gui_task` | Core 1 | 3 | 16384 | E-Ink界面渲染 |
| `fsrs_task` | Core 1 | 2 | 8192 | FSRS算法计算 |
| `db_task` | Core 1 | 2 | 8192 | SQLite数据库操作 |
| `touch_task` | Core 1 | 4 | 2048 | 触摸屏输入处理 |
| `power_task` | Core 0 | 1 | 2048 | 电源状态监测 |

### 4.3 状态机设计

```
                    ┌─────────────┐
                    │   BOOTING   │
                    └──────┬──────┘
                           │
              ┌────────────▼────────────┐
              │     WIFI_CONFIG_CHECK   │
              └────┬──────────────┬─────┘
                   │ 无配置       │ 有配置
         ┌─────────▼─────┐  ┌────▼─────────┐
         │  AP_MODE_ONLY │  │  STA_CONNECT  │
         └──────┬────────┘  └────┬──────────┘
                │                │
         ┌──────▼────────────────▼──────┐
         │        IDLE (主菜单)          │◄──────────────┐
         └──────┬──────────┬────────────┘               │
                │          │                            │
     ┌──────────▼──┐  ┌────▼──────────┐               │
     │  REVIEWING  │  │  WEB_ACTIVE   │               │
     │  (复习模式) │  │  (Web管理)    │               │
     └──────┬──────┘  └────┬──────────┘               │
            │              │                            │
            └──────┬───────┘                            │
                   │                                    │
         ┌─────────▼─────────┐                         │
         │  IDLE_TIMEOUT     │─────────────────────────┘
         │  (15分钟无操作)    │
         └─────────┬─────────┘
                   │
         ┌─────────▼─────────┐
         │    DEEP_SLEEP     │
         │  (触摸唤醒/WiFi唤醒)│
         └───────────────────┘
```

---

## 5. 功能需求详述

### 5.1 模块：系统初始化 (SYS-INIT)

#### FR-SYS-001 固件启动
**优先级**: P0  
**描述**: 设备上电或复位后，在3秒内完成启动并显示主菜单界面。  
**验收标准**:
- 从冷启动到显示主菜单时间 < 3秒。
- 启动画面显示AnkiEInk Logo，使用A2模式快速加载。
- 启动过程中检测Flash完整性（SPIFFS校验）。
- 若数据库损坏，提示用户并进入恢复模式。

#### FR-SYS-002 固件OTA更新
**优先级**: P1  
**描述**: 支持通过Web界面上传固件或从预设URL下载更新。  
**验收标准**:
- Web端上传.bin文件，校验MD5后刷入ota_0/1分区。
- 支持URL下载（填写固件URL，后台下载）。
- 升级过程显示进度条，升级完成自动重启。
- 保留用户数据（牌组、设置），仅升级固件。
- 支持升级失败回滚到上一版本。

#### FR-SYS-003 恢复出厂设置
**优先级**: P1  
**描述**: 清除所有用户数据，恢复到出厂状态。  
**验收标准**:
- Web端触发 + 设备端物理按键（电源键长按5秒）双重确认。
- 清除NVS中所有配置。
- 格式化deck_db分区。
- 保留固件版本，重启进入首次设置向导。

---

### 5.2 模块：电源管理 (PM)

#### FR-PM-001 电池状态监测
**优先级**: P0  
**描述**: 实时监测电池电量、充放电状态，并在UI上显示。  
**验收标准**:
- 通过AXP2101读取电池电压，转换为电量百分比（误差 < 5%）。
- 充电时显示闪电图标，充满显示插头图标。
- 电量 < 10% 时显示低电量警告（状态栏电池图标闪烁）。
- 电量 < 5% 时自动保存状态并进入深度休眠。

#### FR-PM-002 自动休眠策略
**优先级**: P0  
**描述**: 根据用户设置的超时时间，自动进入休眠模式以节省电量。  
**验收标准**:
- 用户可设置超时时间：1/3/5/10/15分钟。
- 超时无操作后，ESP32进入Deep Sleep，IT8951进入Standby。
- 触摸屏中断可唤醒（GT911的INT引脚）。
- 唤醒后恢复休眠前界面，卡片数据不丢失。
- Web活动期间不触发休眠（15分钟Web超时后关闭WiFi再休眠）。

#### FR-PM-003 功耗优化显示
**优先级**: P0  
**描述**: 优化E-Ink刷新策略，减少不必要的全屏刷新。  
**验收标准**:
- 复习卡片评分时，仅局部刷新按钮区域（A2模式）。
- 翻页时使用GC16模式保证清晰度。
- 每5次局部刷新后强制执行一次全屏刷新清除残影。
- 待机状态下IT8951进入Standby模式，功耗 < 500μA。

---

### 5.3 模块：WiFi与网络 (NET)

#### FR-NET-001 WiFi配网
**优先级**: P0  
**描述**: 支持Web配网、AP热点配网两种方式。  
**验收标准**:
- 首次开机无WiFi配置时，自动开启AP模式（SSID: AnkiEInk-XXXX）。
- 连接AP后访问`192.168.4.1`进入配网页面。
- 扫描周围WiFi列表并显示信号强度。
- 输入密码连接，连接成功自动关闭AP，切换到STA模式。
- 密码错误时给予明确提示。
- 支持最多保存 5 组 WiFi 凭据（索引 0~4，存储于 NVS wifi 命名空间），开机时按信号强度 + 优先级自动选择连接，当前激活索引写入 `wifi/current_index`。

#### FR-NET-002 定时网络策略
**优先级**: P0  
**描述**: 为节省电量，WiFi采用定时连接策略。  
**验收标准**:
- 默认每天凌晨3:00自动开启WiFi 2分钟（同步时间+上传复习日志）。
- 用户可自定义同步时间或关闭自动同步。
- 手动进入Web管理模式时立即开启WiFi。
- Web管理15分钟无请求自动关闭WiFi。
- 复习过程中WiFi保持关闭（离线模式）。

#### FR-NET-003 NTP时间同步
**优先级**: P0  
**描述**: 联网后自动同步UTC时间，并转换为用户时区。  
**验收标准**:
- 使用NTP服务器池（默认`pool.ntp.org`）。
- 联网后30秒内完成时间同步。
- 支持自定义时区（Web设置界面选择）。
- 同步失败时使用RTC内部时钟继续计时。
- 同步成功后写入ESP32 RTC模块。

---

### 5.4 模块：Web管理后台 (WEB)

#### FR-WEB-001 HTTP服务器
**优先级**: P0  
**描述**: 在ESP32上运行轻量级HTTP服务器，提供完整的Web管理界面。  
**验收标准**:
- 监听端口80，支持同时2个客户端连接。
- 所有静态资源（HTML/CSS/JS）打包为单个gzip压缩文件，存储于web_res分区。
- 响应头正确设置Content-Encoding: gzip。
- 支持RESTful API，返回JSON格式数据。
- 请求超时时间10秒，防止长时间占用。

#### FR-WEB-002 仪表盘页面
**优先级**: P0  
**描述**: Web管理首页，展示设备状态和复习概览。  
**验收标准**:
- 显示设备电量、存储使用率、WiFi状态。
- 显示今日复习进度（已复习/剩余）。
- 显示连续学习天数、总复习次数。
- 近30天复习趋势折线图（Canvas绘制）。
- 设备运行时间显示。

#### FR-WEB-003 牌组管理页面
**优先级**: P0  
**描述**: 管理所有牌组，支持导入导出。  
**验收标准**:
- 列表显示所有牌组（名称、卡片数、待复习数）。
- 支持上传.apkg文件（分片上传，单次最大512KB）。
- 上传进度实时显示。
- 导入过程WebSocket推送进度。
- 支持导出单个牌组为.apkg（包含复习进度）。
- 支持删除牌组（二次确认）。

#### FR-WEB-004 FSRS参数设置页面
**优先级**: P0  
**描述**: 调整FSRS算法参数，优化记忆效果。  
**验收标准**:
- 滑块调整期望记忆率（0.70-0.99）。
- 设置最大间隔天数（1-365）。
- 显示和编辑FSRS参数向量w（13个浮点数）。
- 一键恢复默认参数。
- 基于历史数据优化参数（需 > 1000条复习记录）。
- 参数修改后立即生效。

#### FR-WEB-005 设备设置页面
**优先级**: P1  
**描述**: 设备全局设置。  
**验收标准**:
- WiFi网络管理（扫描、连接、断开、忘记网络）。
- 时区设置（下拉选择UTC偏移）。
- 电源管理设置（自动休眠时间、WiFi开关策略）。
- 每日复习上限设置（新卡片、复习卡片分别设置）。
- 固件版本显示和OTA升级。

#### FR-WEB-006 统计页面
**优先级**: P1  
**描述**: 详细的复习统计和分析。  
**验收标准**:
- 总览统计（总卡片、总复习、平均记忆率）。
- 每日复习量柱状图（可选7/30/90天范围）。
- 单牌组详细统计。
- 复习时长统计（今日/本周/总计）。
- 评分分布图（重来/困难/良好/简单比例）。
- 导出统计报告为CSV。

---

### 5.5 模块：牌组管理 (DECK)

#### FR-DECK-001 APKG文件解析
**优先级**: P0  
**描述**: 解析Anki牌组文件（.apkg），提取卡片内容和媒体资源。  
**验收标准**:
- 正确解析ZIP格式的.apkg文件。
- 提取collection.anki2（SQLite数据库）。
- 解析cards表、notes表、col表。
- 提取媒体文件（图片、音频索引）到SD卡或Flash。
- 支持Base64内嵌图片的提取和存储。
- 解析错误时给出明确错误信息。
- 支持最大500MB的.apkg文件（流式处理）。

#### FR-DECK-002 卡片内容支持
**优先级**: P0  
**描述**: 渲染Anki卡片HTML内容到E-Ink屏幕。  
**验收标准**:
- 支持HTML标签：`<b>`, `<i>`, `<u>`, `<br>`, `<p>`, `<div>`, `<span>`。
- 支持`<img>`标签（从本地媒体库加载）。
- 支持简单表格`<table>`, `<tr>`, `<td>`。
- 支持列表`<ul>`, `<ol>`, `<li>`。
- 不支持的内容降级为纯文本显示。
- 忽略`<script>`, `<style>`, CSS复杂定位。
- 特殊字符和Unicode正确显示（需包含必要字体）。

#### FR-DECK-003 数据库存储
**优先级**: P0  
**描述**: 在ESP32 Flash上使用SQLite存储牌组数据。  
**验收标准**:
- 使用SPIFFS挂载deck_db分区。
- SQLite数据库启用WAL模式。
- 存储卡片表（id, deck_id, front_html, back_html, media_refs）。
- 存储卡片状态表（card_id, stability, difficulty, due, state, reps, lapses, ...）。
- 存储复习日志表（card_id, timestamp, rating, elapsed_ms）。
- 存储媒体文件映射表（filename, data_blob/sd_path）。
- 定期执行PRAGMA integrity_check（每天凌晨）。

---

### 5.6 模块：FSRS算法 (FSRS)

#### FR-FSRS-001 FSRS V5核心算法
**优先级**: P0  
**描述**: 实现FSRS V5算法，计算卡片的下次复习时间。  
**验收标准**:
- 严格遵循FSRS V5标准公式。
- 使用ESP32-S3硬件FPU进行浮点运算。
- 支持4种评分映射：Again(1), Hard(2), Good(3), Easy(4)。
- 根据评分计算新的Stability和Difficulty。
- 计算下次复习间隔（天）。
- 支持卡片状态：New, Learning, Review, Relearning。
- 单次计算时间 < 50ms。

#### FR-FSRS-002 参数配置
**优先级**: P0  
**描述**: 支持FSRS参数的导入、导出和自定义。  
**验收标准**:
- 默认使用FSRS内置基准参数（request_retention=0.9）。
- 支持13个w参数的精确设置。
- 支持请求保留率(request_retention)调整。
- 支持最大间隔(maximum_interval)设置。
- 参数存储于NVS，掉电不丢失。
- 支持导入/导出参数为JSON。

#### FR-FSRS-003 参数优化
**优先级**: P2  
**描述**: 基于历史复习记录优化FSRS参数。  
**验收标准**:
- 收集至少1000条复习记录后可使用优化功能。
- 优化算法使用最小化均方误差（MSE）。
- 优化过程在Core 1后台执行，不阻塞UI。
- 优化结果显示RMSE和参数变化对比。
- 用户可选择接受或拒绝优化结果。

#### FR-FSRS-004 每日调度
**优先级**: P0  
**描述**: 根据卡片到期时间，生成每日复习队列。  
**验收标准**:
- 每日0点自动更新到期卡片列表。
- 复习队列按到期时间排序（最紧急的优先）。
- 新卡片按添加顺序排列。
- 支持每日新卡片上限（默认20张）。
- 支持每日复习上限（默认200张）。
- 超限卡片自动推迟到次日。

---

### 5.7 模块：复习交互 (REVIEW)

#### FR-REV-001 复习流程
**优先级**: P0  
**描述**: 完整的卡片复习交互流程。  
**验收标准**:
1. 显示卡片正面（问题），使用GC16模式保证清晰度。
2. 用户点击屏幕任意位置，翻转显示答案背面（门帘刷新效果）。
3. 答案下方显示4个评分按钮：重来/困难/良好/简单。
4. 用户点击评分按钮后：
   - A2模式局部刷新按钮区域（显示选中状态）。
   - 调用FSRS算法计算新状态。
   - 保存到数据库。
   - 自动加载下一张卡片。
5. 每日复习完成后显示完成页面（统计本次复习数据）。

#### FR-REV-002 撤回功能
**优先级**: P1  
**描述**: 支持撤销上一次评分操作。  
**验收标准**:
- 评分后3秒内在屏幕底部显示"撤回"按钮。
- 点击撤回恢复卡片到复习前状态。
- 撤回后退回该卡片重新评分。
- 仅支持撤回最近1次操作。

#### FR-REV-003 复习统计实时显示
**优先级**: P1  
**描述**: 复习过程中显示实时统计信息。  
**验收标准**:
- 顶部状态栏显示今日进度（已复习/总到期）。
- 顶部显示当前牌组名称。
- 进度条实时更新。
- 支持查看当前卡片的历史复习记录（长按卡片）。

#### FR-REV-004 断点续复
**优先级**: P0  
**描述**: 复习中断（休眠、关机）后恢复时，从上次位置继续。  
**验收标准**:
- 每次评分后立即持久化卡片状态。
- 退出复习时保存当前队列位置。
- 重新进入复习时恢复队列。
- 暂停超过4小时的卡片视为新会话，重新排列队列。

---

## 6. 非功能需求

### 6.1 性能需求

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 冷启动时间 | < 3秒 | 示波器测量上电到主界面时间 |
| 卡片翻页延迟 | < 400ms | 触摸中断到屏幕更新完成 |
| FSRS计算时间 | < 50ms | 单次scheduler调用计时 |
| API响应时间 | < 200ms | 95%请求在此时间内 |
| SQLite查询时间 | < 100ms | 单表10000行内查询 |
| APKG导入速度 | > 100张/秒 | 不含媒体文件 |

### 6.2 功耗需求

| 模式 | 平均电流 | 说明 |
|------|----------|------|
| 深度休眠 | < 500μA | RTC+触摸唤醒 |
| 待机（WiFi关） | < 10mA | 显示静态界面 |
| 复习模式 | < 80mA | 频繁局部刷新 |
| Web管理模式 | < 150mA | WiFi+HTTP服务 |
| 峰值（全刷+WiFi） | < 500mA | 短暂峰值 |

### 6.3 容量需求

| 资源 | 容量 | 说明 |
|------|------|------|
| 牌组存储 | 6MB Flash + SD卡 | 约10000-15000张纯文本卡片 |
| 媒体文件 | SD卡（推荐32GB） | 图片、音频资源 |
| 复习日志 | 保留90天 | 自动清理旧日志 |
| WiFi凭据 | 最多5个 | NVS存储 |

### 6.4 可靠性需求

- **数据安全**: 每次评分后立即写入数据库（WAL模式）。
- **异常恢复**: 崩溃后重启自动恢复上次状态。
- **断电保护**: SQLite WAL日志保证数据一致性。
- **运行稳定性**: 7×24小时连续运行不死机。
- **错误率**: 评分计算错误率 < 0.01%。

### 6.5 兼容性需求

- 兼容Anki 2.1.50+版本导出的.apkg文件。
- 支持AnkiDroid和Anki Desktop的卡片格式。
- 卡片HTML兼容主流标签（详见FR-DECK-002）。
- Web界面兼容Chrome/Safari/Firefox移动版和桌面版。

### 6.6 安全与隐私需求

- **6.6.1 Web 后台鉴权**：Basic Auth 凭证存储于 NVS settings 命名空间，默认用户名 admin，首次登录强制修改密码。密码使用 SHA-256 + Salt 哈希存储。登录成功后签发 2 小时有效 Token。
- **6.6.2 敏感数据保护**：WiFi 密码存储前使用 AES-128-ECB 加密（密钥为芯片 MAC 地址），明文不写入日志。
- **6.6.3 数据备份与恢复**：每日凌晨备份 SQLite 数据库至 SD 卡 /backup/ 目录（保留最近 7 份），Web 端可手动触发备份与恢复。断电恢复采用 WAL 前滚 + 校验和验证。
- **6.6.4 隐私合规**：不收集任何个人数据，全部数据本地存储。APKG 导入文件解析完成后临时文件立即删除。无遥测、无广告、无云端传输。

---

## 7. 用户界面规范

### 7.1 E-Ink屏幕布局规范

```
┌──────────────────────────────────────┐
│ [状态栏]  08:30     📶    🔋 85%   │ 高度: 40px
├──────────────────────────────────────┤
│                                      │
│  [主内容区域]                        │  高度: 440px
│   - 菜单列表                        │
│   - 卡片内容                        │
│   - 设置选项                        │
│                                      │
├──────────────────────────────────────┤
│ [底部操作栏]                        │  高度: 60px
│  [按钮1]  [按钮2]  [按钮3]  [按钮4] │
└──────────────────────────────────────┘

总分辨率: 960×540
```

### 7.2 刷新模式策略

| 场景 | 模式 | 理由 |
|------|------|------|
| 菜单切换 | GC16 | 清晰度优先，整页刷新 |
| 卡片正面 | GC16 | 保证文字清晰 |
| 卡片翻转 | DU4 + 门帘效果 | 视觉效果好 |
| 评分按钮按下 | A2 | 快速响应 |
| 局部进度更新 | A2 | 无残影顾虑 |
| 统计图表 | GC16 | 细节清晰 |
| 每5次A2刷新后 | GC16 | 清除残影 |

### 7.3 字体规范

字体方案参考电子书开源项目 [crosspoint-reader-papers3-fork](https://github.com/byronwind/crosspoint-reader-papers3) 的 App 框架与 CJK 字体移植实现，采用分层加载 + 字符回退策略。以下为具体规范：

#### 7.3.1 内置字体方案（固件内置，不可删除）
| 字体文件名 | 覆盖字符集 | 字重 | 用途 | 存储位置 |
|-----------|-----------|------|------|---------|
| `NotoSansSC-Regular.otf` | 简体中文（GB2312+扩展） + 英文 + 常用符号 | Regular 400 | 菜单、按钮、注释 | Flash web_res/FAT 分区字体子目录 |
| `NotoSansSC-Bold.otf` | 同上 | Bold 700 | 标题、强调文字 | 同上 |
| `NotoSerifSC-Regular.otf` | 简体中文 + 英文 | Regular 400 | 卡片问题/答案正文（衬线体可选） | 同上 |
| `NotoSansJP-Regular.otf` | 日文假名 + 常用汉字 | Regular 400 | 日语学习卡片回退 | 同上 |
| `DejaVuSansMono.ttf` | 英文等宽 + 数字 | Regular 400 | 状态栏时间、代码片段显示 | 同上 |

> 内置字体采用子集化压缩（仅保留 3500 常用汉字 + 假名 + 符号），合计占用 ≤ 1.2MB Flash 空间。

#### 7.3.2 第三方字体扩展（SD 卡加载）
用户可将自定义字体存放于 SD 卡 `/AnkiEInk/fonts/` 目录（参见 3.4.2 节），支持格式 `.ttf` / `.otf` / `.ttc`（取第 1 个子字体）。加载规则：
- 字体发现：启动时扫描 `/AnkiEInk/fonts/` 下所有字体文件，读取字体名与字重元数据。
- 命名规范：推荐使用 `{FamilyName}-{Weight}.ttf`，如 `SourceHanSansCN-Medium.ttf`。
- 总大小限制：单字体 ≤ 32MB，fonts 目录总占用 ≤ 512MB。
- Web 设置页面可切换：正文衬线/正文无衬线/标题字重 三类独立选择。

#### 7.3.3 字体加载优先级与 CJK 字符回退策略
针对卡片内容中出现的每一个 Unicode 字符，按以下顺序查找可用字形（Glyph），全部失败则显示 □（缺字占位符）：
1. **用户首选字体**：若用户已在 Web 设置中指定正文字体且字符存在 → 使用。
2. **卡片指定字体**：解析 HTML `<span style="font-family:xxx">` 标签匹配 SD 卡字体。
3. **内置主字体回退**：NotoSansSC → NotoSerifSC → NotoSansJP（顺序按语言探测）。
4. **最后回退**：DejaVuSansMono（纯英文/数字/符号兜底）。

> CJK 语言探测优先使用卡片所在牌组元数据（APKG col.conf 中的 `deckConf->name` 关键词匹配 "日本語"/"한국어"/"English"），无匹配时默认简体中文优先。

#### 7.3.4 字号规范（与原文档保持一致，仅重述格式）
| 用途 | 字号 | 推荐字体族 | 说明 |
|------|------|-----------|------|
| 状态栏 | 16px | 等宽 (DejaVuSansMono) | 时间、电量、WiFi 图标 |
| 卡片问题 / 答案正文 | 24px | 衬线或无衬线（用户可选） | 主要阅读内容，可 Web 设置中调整 ±6px |
| 评分按钮文字 | 20px | 无衬线 Bold | 重来 / 困难 / 良好 / 简单 |
| 菜单项 | 22px | 无衬线 Regular | 主菜单、设置项列表 |
| 小字注释 | 14px | 无衬线 Regular | 辅助信息、统计标签 |

### 7.4 Web界面设计原则

- 响应式设计，适配手机(320px)到桌面(1920px)。
- 暗色主题为主（减少设备屏幕光干扰）。
- 所有操作提供明确反馈（Toast提示）。
- 危险操作（删除）二次确认。
- 实时数据通过WebSocket推送更新。

---

## 8. 数据模型设计

### 8.1 SQLite数据库Schema

```sql
-- 牌组表
CREATE TABLE decks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    created_at INTEGER NOT NULL,  -- Unix timestamp
    updated_at INTEGER NOT NULL
);

-- 卡片表
CREATE TABLE cards (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    deck_id INTEGER NOT NULL,
    front_html TEXT NOT NULL,
    back_html TEXT NOT NULL,
    front_text TEXT,              -- 纯文本备用
    back_text TEXT,               -- 纯文本备用
    media_refs TEXT,              -- JSON数组: ["img1.jpg","img2.png"]
    created_at INTEGER NOT NULL,
    FOREIGN KEY (deck_id) REFERENCES decks(id) ON DELETE CASCADE
);

-- 卡片状态表 (FSRS)
CREATE TABLE card_states (
    card_id INTEGER PRIMARY KEY,
    state INTEGER NOT NULL DEFAULT 0,    -- 0:New, 1:Learning, 2:Review, 3:Relearning
    stability REAL NOT NULL DEFAULT 0,
    difficulty REAL NOT NULL DEFAULT 0,
    due INTEGER NOT NULL,                -- 到期时间戳
    last_review INTEGER,                 -- 上次复习时间戳
    elapsed_days REAL DEFAULT 0,
    scheduled_days REAL DEFAULT 0,
    reps INTEGER DEFAULT 0,
    lapses INTEGER DEFAULT 0,
    FOREIGN KEY (card_id) REFERENCES cards(id) ON DELETE CASCADE
);

-- 复习日志表
CREATE TABLE review_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    card_id INTEGER NOT NULL,
    deck_id INTEGER NOT NULL,
    timestamp INTEGER NOT NULL,
    rating INTEGER NOT NULL,             -- 1:Again, 2:Hard, 3:Good, 4:Easy
    elapsed_ms INTEGER,                  -- 回答用时(毫秒)
    state_before INTEGER,                -- 复习前状态
    state_after INTEGER,                 -- 复习后状态
    stability_before REAL,
    stability_after REAL,
    difficulty_before REAL,
    difficulty_after REAL,
    FOREIGN KEY (card_id) REFERENCES cards(id) ON DELETE CASCADE
);

-- 媒体文件索引表
CREATE TABLE media (
    filename TEXT PRIMARY KEY,
    size INTEGER,
    storage_path TEXT,                   -- SD卡路径或Flash偏移
    checksum TEXT
);

-- 复习会话表
CREATE TABLE review_sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    deck_id INTEGER NOT NULL,
    start_time INTEGER NOT NULL,
    end_time INTEGER,
    cards_reviewed INTEGER DEFAULT 0,
    FOREIGN KEY (deck_id) REFERENCES decks(id) ON DELETE CASCADE
);

-- 索引
CREATE INDEX idx_cards_deck ON cards(deck_id);
CREATE INDEX idx_card_states_due ON card_states(due);
CREATE INDEX idx_review_logs_card ON review_logs(card_id);
CREATE INDEX idx_review_logs_timestamp ON review_logs(timestamp);
```

### 8.2 NVS存储键值

| 命名空间 | 键 | 类型 | 说明 |
|----------|-----|------|------|
| `wifi` | `sta_ssid_0` | string | WiFi SSID（第1组） |
| `wifi` | `sta_pass_0` | string | WiFi 密码（第1组，AES加密存储） |
| `wifi` | `sta_ssid_1` | string | WiFi SSID（第2组） |
| `wifi` | `sta_pass_1` | string | WiFi 密码（第2组，AES加密存储） |
| `wifi` | `sta_ssid_2` | string | WiFi SSID（第3组） |
| `wifi` | `sta_pass_2` | string | WiFi 密码（第3组，AES加密存储） |
| `wifi` | `sta_ssid_3` | string | WiFi SSID（第4组） |
| `wifi` | `sta_pass_3` | string | WiFi 密码（第4组，AES加密存储） |
| `wifi` | `sta_ssid_4` | string | WiFi SSID（第5组） |
| `wifi` | `sta_pass_4` | string | WiFi 密码（第5组，AES加密存储） |
| `wifi` | `current_index` | u8 | 当前激活 WiFi 组索引（0~4，默认 0xFF 表示未选） |
| `wifi` | `ap_ssid` | string | AP 热点名称（格式：AnkiEInk-XXXX） |
| `wifi` | `ap_password` | string | AP 热点密码（≥8 位，默认空表示开放） |
| `settings` | `timezone` | i8 | UTC偏移小时 |
| `settings` | `auto_sync` | u8 | 自动同步开关 |
| `settings` | `sync_hour` | u8 | 同步时间(0-23) |
| `settings` | `sleep_timeout` | u16 | 休眠超时(秒) |
| `settings` | `daily_new_limit` | u16 | 每日新卡上限 |
| `settings` | `daily_review_limit` | u16 | 每日复习上限 |
| `fsrs` | `request_retention` | float | 期望记忆率 |
| `fsrs` | `maximum_interval` | u32 | 最大间隔(天) |
| `fsrs` | `w_params` | blob | 13个float参数 |
| `device` | `boot_count` | u32 | 启动次数 |
| `device` | `total_review_time` | u64 | 总复习时长(秒) |

---

## 9. API接口规范

### 9.1 通用规范

**Base URL**: `http://[device-ip]/api`

**响应格式**:
```json
{
  "code": 0,           // 0:成功, 非0:错误码
  "message": "ok",     // 错误时描述信息
  "data": {}           // 响应数据
}
```

**错误码定义（结构化矩阵，按领域分层，符合 Error Code Matrix 规范）**

通用响应结构保持不变，错误码按 5 大领域分层（1xxx 系统 / 2xxx 网络 / 3xxx 牌组 / 4xxx 算法 / 5xxx 设备）。
关键外部操作（OTA / 同步 / 备份）默认内置 **3 次指数退避重试（1s/2s/4s）**。

| 错误码 | 名称(Name) | 说明(Description) | 可重试性 | 处理建议(Recommendation) | 归属领域 |
|--------|-----------|------------------|---------|-------------------------|---------|
| 0 | OK | 操作成功 | - | - | 通用 |
| 1001 | INVALID_PARAM | 请求参数缺失或格式非法 | 否 | 检查必填字段、类型、范围，对照 API 文档修正 | 系统 |
| 1002 | RESOURCE_NOT_FOUND | 请求的资源不存在（牌组/卡片/文件） | 否 | 确认 ID 是否正确，资源是否已被删除 | 系统 |
| 1003 | OPERATION_DENIED | 操作不允许（越权/时机不当） | 否 | 登录鉴权，或等待当前操作（如导入中）完成后重试 | 系统 |
| 1004 | INTERNAL_ERROR | 未分类内部异常 | 是（最多3次） | 记录错误上下文，重启后重试；连续失败上报 | 系统 |
| 1005 | DB_CORRUPTED | SQLite 数据库完整性校验失败 | 条件 | 触发从 SD 卡 /backup/ 自动恢复最近 7 份镜像 | 系统 |
| 2001 | WIFI_CONNECT_FAIL | WiFi STA 连接超时（30s无响应） | 是（最多5次，切换信道） | 检查密码正确性，靠近路由器；手动指定 BSSID | 网络 |
| 2002 | WIFI_WRONG_PASSWORD | WiFi 密码认证失败 | 否 | 重新输入正确密码后连接 | 网络 |
| 2003 | WIFI_NO_AP_FOUND | 配网扫描未找到目标 SSID | 是（间隔5s重扫） | 确认路由器 2.4G 开启，SSID 是否隐藏 | 网络 |
| 2004 | NTP_SYNC_FAIL | NTP 时间同步超时 | 是（3次退避） | 检查默认 pool.ntp.org 可访问或配置内网 NTP | 网络 |
| 3001 | APKG_FORMAT_ERROR | APKG 文件非合法 ZIP 或缺少 collection.anki2 | 否 | 使用 Anki 官方工具重新导出牌组后再导入 | 牌组 |
| 3002 | APKG_PARSE_FAIL | APKG 解析过程 SQLite 读取/解压失败 | 否 | 检查文件完整性 MD5，确认未损坏后重新上传 | 牌组 |
| 3003 | STORAGE_INSUFFICIENT | Flash 或 SD 卡可用空间低于导入所需 | 条件（清理后可重试） | 删除旧牌组备份或转移媒体文件至大容量 SD | 牌组 |
| 3004 | CARD_RENDER_FAIL | 卡片 HTML 渲染引擎崩溃 | 是（降级纯文本） | 自动回退至 front_text/back_text 纯文本显示 | 牌组 |
| 4001 | FSRS_PARAM_INVALID | FSRS w 参数或保留率超出合法区间 | 否 | 一键恢复默认参数或手动修正 13 维向量范围 | 算法 |
| 4002 | FSRS_OPTIMIZE_NO_DATA | 参数优化触发时复习记录不足 1000 条 | 否 | 继续积累复习数据，达到阈值后重新尝试优化 | 算法 |
| 5001 | EINK_REFRESH_FAIL | IT8951 刷新指令返回异常 | 是（复位驱动） | 触发软复位 IT8951，重新初始化 SPI 总线 | 设备 |
| 5002 | AXP_PMU_FAIL | AXP2101 电池读数异常或充放电芯片通信失败 | 是（重读寄存器） | 连续失败 3 次后提示电源异常，建议售后 | 设备 |
| 5003 | OTA_CHECKSUM_FAIL | OTA 固件 MD5 校验不通过 | 是（重新下载） | 删除缓存固件，重新上传或从可靠 URL 下载 | 设备 |
| 5004 | SD_CARD_MISSING | 操作 SD 卡依赖功能时未检测到卡插入 | 否 | 插入 FAT32 格式 MicroSD（最大 32GB）后重试 | 设备 |

### 9.2 完整API列表

#### 系统状态
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/status` | 设备状态总览 |
| GET | `/api/device/info` | 硬件信息 |
| POST | `/api/device/reboot` | 重启设备 |
| POST | `/api/device/sleep` | 立即休眠 |
| POST | `/api/device/factory-reset` | 恢复出厂设置 |

#### WiFi管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/wifi/status` | 当前连接状态 |
| GET | `/api/wifi/scan` | 扫描可用网络 |
| POST | `/api/wifi/connect` | 连接网络 |
| POST | `/api/wifi/disconnect` | 断开网络 |
| DELETE | `/api/wifi/forget` | 忘记网络 |

#### 牌组管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/decks` | 获取牌组列表 |
| GET | `/api/decks/:id` | 获取牌组详情 |
| POST | `/api/decks/upload` | 上传APKG文件 |
| POST | `/api/decks/upload/chunk` | 分片上传 |
| POST | `/api/decks/import` | 触发导入处理 |
| GET | `/api/decks/import/progress` | 导入进度 |
| GET | `/api/decks/:id/export` | 导出牌组 |
| DELETE | `/api/decks/:id` | 删除牌组 |

#### 复习设置
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/settings/review` | 获取复习设置 |
| PUT | `/api/settings/review` | 更新复习设置 |
| GET | `/api/settings/fsrs` | 获取FSRS参数 |
| PUT | `/api/settings/fsrs` | 更新FSRS参数 |
| POST | `/api/settings/fsrs/optimize` | 优化FSRS参数 |
| GET | `/api/settings/fsrs/optimize/status` | 优化进度 |
| POST | `/api/settings/fsrs/reset` | 恢复默认参数 |

#### 统计
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stats/overview` | 总览统计 |
| GET | `/api/stats/daily` | 每日统计(支持?days=30) |
| GET | `/api/stats/decks/:id` | 单牌组统计 |
| GET | `/api/stats/logs` | 复习日志(分页) |
| GET | `/api/stats/export` | 导出统计CSV |

#### 时间同步
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/time` | 获取当前时间 |
| POST | `/api/time/sync` | 强制NTP同步 |
| PUT | `/api/time/timezone` | 设置时区 |

#### OTA更新
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ota/upload` | 上传固件 |
| POST | `/api/ota/url` | 从URL下载固件 |
| GET | `/api/ota/status` | 更新进度 |

### 9.3 WebSocket事件定义

**连接**: `ws://[device-ip]/ws`

**服务端→客户端事件**:

| 事件 | 数据结构 | 说明 |
|------|----------|------|
| `sync_progress` | `{type, current, total, message}` | 同步/导入进度 |
| `device_status` | `{battery, charging, wifi_rssi, time}` | 设备状态(30秒间隔) |
| `review_event` | `{card_id, rating, due}` | 设备端复习事件 |
| `ota_progress` | `{percent, status}` | OTA更新进度 |
| `wifi_scan_result` | `[{ssid, rssi, security}]` | WiFi扫描结果 |
| `import_complete` | `{deck_id, cards_imported}` | 导入完成通知 |
| `error` | `{code, message}` | 错误通知 |

**客户端→服务端事件**:

| 事件 | 数据结构 | 说明 |
|------|----------|------|
| `ping` | `{}` | 心跳 |
| `sync_time` | `{}` | 触发时间同步 |
| `scan_wifi` | `{}` | 触发WiFi扫描 |

---

## 10. 测试策略

### 10.1 测试层级

```
┌─────────────────────────────────────┐
│         端到端测试 (E2E)            │
│   完整用户场景，真实硬件测试        │
├─────────────────────────────────────┤
│         集成测试                    │
│   模块间交互：FSRS+DB, HTTP+DB     │
├─────────────────────────────────────┤
│         单元测试                    │
│   FSRS算法, APKG解析, 状态机       │
└─────────────────────────────────────┘
```

### 10.2 单元测试用例 (关键)

| 模块 | 测试用例 | 预期结果 |
|------|----------|----------|
| FSRS | 新卡片首次评分Good | state→Review, interval≈1-3天 |
| FSRS | 连续评分Again | difficulty增加, stability降低 |
| FSRS | 参数边界测试(极限值) | 不崩溃，返回合理值 |
| APKG解析 | 解析标准.apkg文件 | 正确提取卡片数和媒体文件 |
| APKG解析 | 解析损坏的.apkg | 返回错误码，不崩溃 |
| APKG解析 | 空牌组导入 | 提示空牌组 |
| 状态机 | DeepSleep→Wake | 正确恢复状态 |
| 状态机 | 复习中断→恢复 | 队列位置正确 |
| SQLite | 10000条卡片查询 | < 100ms |

### 10.3 集成测试场景

| 场景 | 步骤 | 验证点 |
|------|------|--------|
| 完整复习流程 | 导入牌组→复习10张卡片→查看统计 | 卡片调度正确，统计数据准确 |
| WiFi配网 | 首次开机→AP配网→连接WiFi→同步时间 | WiFi连接成功，时间正确 |
| Web管理 | 开启Web→上传牌组→修改FSRS参数→查看仪表盘 | 各API正常响应 |
| 断电恢复 | 复习中途断电→重启→继续复习 | 卡片状态不丢失，队列恢复 |
| 并发访问 | Web端操作同时设备端复习 | 无数据冲突 |

### 10.4 性能测试

| 测试项 | 方法 | 通过标准 |
|--------|------|----------|
| 启动时间 | 示波器/GPIO测量 | < 3秒 |
| 卡片翻转延迟 | 高速相机拍摄 | < 400ms |
| 10000张卡片导入 | 计时 | < 100秒 |
| 7天连续运行 | 自动化复习脚本 | 无崩溃、无内存泄漏 |
| 功耗测试 | 电流表串联测量 | 休眠<500μA, 复习<80mA |

### 10.5 兼容性测试

| 测试项 | 测试数据 |
|--------|----------|
| APKG兼容性 | Anki官方示例牌组、AnkiDroid导出的牌组、AnkiWeb下载的热门牌组（至少10个） |
| Web浏览器 | Chrome/Safari/Firefox 最新版 移动端+桌面端 |
| HTML卡片 | 含表格、图片、LaTeX(MathJax降级)、日文/中文混排的卡片 |

---

## 11. 开发计划与里程碑

### 11.1 总体时间线

```
项目总周期: 12周 (约3个月)
  ├── Phase 0: 环境搭建 (Week 1)
  ├── Phase 1: 核心MVP (Week 2-4)
  ├── Phase 2: Web管理 (Week 5-7)
  ├── Phase 3: 完善优化 (Week 8-10)
  └── Phase 4: 测试发布 (Week 11-12)
```

---

### 11.2 Phase 0: 环境搭建与原型验证

**周期**: Week 1 (8月18日 - 8月24日)

#### Milestone M0: 开发环境就绪 (Day 3)
| 任务ID | 任务 | 交付物 | 验收标准 |
|--------|------|--------|----------|
| M0-T1 | PIO/ESP-IDF环境搭建 | 编译脚本 | 成功编译hello_world烧录运行 |
| M0-T2 | M5PaperS3硬件验证 | 硬件测试报告 | 屏幕、触摸、PMU、SD卡均正常 |
| M0-T3 | Flash分区表创建 | partitions.csv | 分区烧录成功，SPIFFS挂载正常 |
| M0-T4 | 基础显示驱动移植 | IT8951驱动封装 | A2和GC16模式刷新正常 |
| M0-T5 | SQLite编译集成 | 测试程序 | CRUD操作正常，WAL模式启用 |

#### Milestone M0.5: 原型验证 (Day 5)
| 任务ID | 任务 | 交付物 | 验收标准 |
|--------|------|--------|----------|
| M0-T6 | 显示Hello World | 固件 | 屏幕显示文字，触摸有响应 |
| M0-T7 | 电源管理基础功能 | 电池读取demo | 正确读取电池电量和充电状态 |
| M0-T8 | FSRS算法C移植 | 单元测试 | 对照Python版本，输出结果一致 |

---

### 11.3 Phase 1: 核心MVP

**周期**: Week 2-4 (8月25日 - 9月14日)

#### Milestone M1: 基础框架 (Week 2结束)
**目标**: 设备能开机运行，显示主菜单，能进入复习（使用硬编码测试卡片）

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M1-T1 | FreeRTOS多任务框架 | 任务调度代码 | M0 |
| M1-T2 | 状态机实现 | 状态切换逻辑 | M1-T1 |
| M1-T3 | GUI框架搭建 | 主菜单/复习界面 | M0-T6 |
| M1-T4 | 触摸事件处理 | 触摸驱动+事件分发 | M0-T6 |
| M1-T5 | 电源管理任务 | 电量监测+休眠逻辑 | M0-T7 |
| M1-T6 | SQLite Schema创建 | 建表脚本 | M0-T5 |

#### Milestone M2: 离线复习功能 (Week 3结束)
**目标**: 能手动导入测试卡片，完成完整的复习流程

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M2-T1 | APKG解析模块 | ZIP解压+SQLite读取 | M1-T6 |
| M2-T2 | 卡片HTML渲染引擎 | HTML→位图转换 | M1-T3 |
| M2-T3 | FSRS调度器 | review调度逻辑 | M0-T8 |
| M2-T4 | 复习交互流程 | 问题→翻转→评分→下一张 | M1-T3, M2-T2, M2-T3 |
| M2-T5 | 数据库读写层 | DAO封装 | M1-T6 |
| M2-T6 | 每日调度 | 到期卡片队列生成 | M2-T3 |

#### Milestone M3: 最小可用版本 (Week 4结束)
**目标**: 设备可独立使用，完成APKG导入+复习+基础设置

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M3-T1 | SD卡文件支持 | SD卡读写 | M0 |
| M3-T2 | APKG从SD卡导入 | 文件选择+导入流程 | M2-T1, M3-T1 |
| M3-T3 | 基础设置页面 | E-Ink设置界面 | M1-T3 |
| M3-T4 | 复习撤回功能 | 操作快照+回滚 | M2-T4 |
| M3-T5 | 复习统计（本地） | 今日统计页面 | M2-T5 |
| M3-T6 | 集成测试 | 测试报告 | 所有M3任务 |

**Phase 1 可交付物: 可独立使用的离线Anki复习设备**

---

### 11.4 Phase 2: Web管理后台

**周期**: Week 5-7 (9月15日 - 10月5日)

#### Milestone M4: Web服务框架 (Week 5结束)
**目标**: HTTP服务器运行，能提供基本API和Web页面

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M4-T1 | HTTP Server搭建 | httpd配置 | M0 |
| M4-T2 | 路由注册框架 | RESTful路由 | M4-T1 |
| M4-T3 | WebSocket服务 | WS连接管理 | M4-T1 |
| M4-T4 | 静态资源服务 | gzip+SPIFFS读取 | M4-T1 |
| M4-T5 | Web前端框架搭建 | 单页面骨架 | - |
| M4-T6 | 仪表盘API | /api/status实现 | M4-T2, M1-T5 |

#### Milestone M5: Web完整功能 (Week 6结束)
**目标**: Web端可完成所有设备管理操作

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M5-T1 | WiFi管理API | 扫描/连接/断开 | M4-T2 |
| M5-T2 | 牌组管理API | CRUD+上传+导出 | M4-T2, M3-T2 |
| M5-T3 | 牌组管理前端页面 | 上传/列表/详情 | M4-T5, M5-T2 |
| M5-T4 | FSRS参数API | 读写+优化触发 | M4-T2, M2-T3 |
| M5-T5 | FSRS设置前端页面 | 参数编辑界面 | M4-T5, M5-T4 |
| M5-T6 | WebSocket实时推送 | 状态/进度事件 | M4-T3 |

#### Milestone M6: Web管理完整版 (Week 7结束)
**目标**: Web管理后台功能齐全，体验流畅

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M6-T1 | 统计API | 各维度统计接口 | M4-T2, M2-T5 |
| M6-T2 | 统计前端页面 | 图表+报表 | M4-T5, M6-T1 |
| M6-T3 | 设备设置前端页面 | 完整设置界面 | M4-T5 |
| M6-T4 | OTA更新 | 上传+URL下载+回滚 | M4-T2 |
| M6-T5 | 响应式适配 | 手机/桌面适配 | M4-T5 |
| M6-T6 | Web+设备协同测试 | 集成测试 | 所有M6任务 |

**Phase 2 可交付物: 带完整Web管理后台的固件**

---

### 11.5 Phase 3: 完善与优化

**周期**: Week 8-10 (10月6日 - 10月26日)

#### Milestone M7: 功耗与性能优化 (Week 8结束)
**目标**: 续航达到目标，操作响应达标

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M7-T1 | 深度休眠优化 | 功耗测试报告 | M1-T5 |
| M7-T2 | IT8951波形优化 | 刷新模式策略 | M1-T3 |
| M7-T3 | E-Ink残影处理 | 全刷策略 | M7-T2 |
| M7-T4 | 定时WiFi策略 | 自动同步逻辑 | M4-T1 |
| M7-T5 | 数据库查询优化 | 索引+查询优化 | M2-T5 |
| M7-T6 | 内存优化 | 内存使用分析 | 全局 |

#### Milestone M8: 体验优化 (Week 9结束)
**目标**: 用户交互体验流畅，细节完善

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M8-T1 | 启动速度优化 | < 3秒启动 | M7 |
| M8-T2 | 卡片渲染优化 | 排版美化 | M2-T2 |
| M8-T3 | 门帘翻页效果 | DU4模式动画 | M7-T2 |
| M8-T4 | 触摸响应优化 | 延迟 < 50ms | M1-T4 |
| M8-T5 | 错误处理完善 | 全模块错误处理 | 全局 |
| M8-T6 | 用户引导流程 | 首次开机引导 | M1-T3 |

#### Milestone M9: 功能补全 (Week 10结束)
**目标**: 所有PRD功能实现完毕

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M9-T1 | FSRS参数优化算法 | 优化器实现 | M2-T3 |
| M9-T2 | 复习日志导出CSV | /api/stats/export | M6-T1 |
| M9-T3 | AnkiConnect协议桥接 | PC同步支持 | M4-T2 |
| M9-T4 | 媒体文件管理 | 清理+预览 | M2-T1 |
| M9-T5 | 多语言支持框架 | i18n基础 | M1-T3 |
| M9-T6 | Web暗色主题 | CSS主题切换 | M4-T5 |

**Phase 3 可交付物: 功能完整、性能达标、体验良好的固件**

---

### 11.6 Phase 4: 测试与发布

**周期**: Week 11-12 (10月27日 - 11月9日)

#### Milestone M10: 质量保证 (Week 11结束)
**目标**: 通过所有测试用例，修复Bug

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M10-T1 | 单元测试完整覆盖 | 测试报告 | M9 |
| M10-T2 | 集成测试 | 测试报告 | M9 |
| M10-T3 | 性能基准测试 | 性能数据 | M7 |
| M10-T4 | 功耗基准测试 | 功耗数据 | M7 |
| M10-T5 | 兼容性测试 | 兼容性报告 | M9 |
| M10-T6 | Bug修复 | 问题跟踪表 | M10-T1~T5 |

#### Milestone M11: 发布准备 (Week 12结束)
**目标**: 发布V1.0正式版

| 任务ID | 任务 | 交付物 | 依赖 |
|--------|------|--------|------|
| M11-T1 | 用户文档编写 | 使用说明书 | M9 |
| M11-T2 | 开发者文档 | API文档+架构文档 | M9 |
| M11-T3 | 发布固件构建 | factory.bin+ota.bin | M10 |
| M11-T4 | 发布包打包 | GitHub Release | M11-T3 |
| M11-T5 | 演示视频录制 | 功能演示 | M11-T3 |
| M11-T6 | 社区发布 | Reddit/Hackaday/... | M11-T4 |

**Phase 4 可交付物: V1.0正式版固件+完整文档**

---

### 11.7 里程碑总览图

```
Week 1   Week 2   Week 3   Week 4   Week 5   Week 6   Week 7   Week 8   Week 9   Week 10  Week 11  Week 12
├────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┤
│  M0    │  M1    │  M2    │  M3    │  M4    │  M5    │  M6    │  M7    │  M8    │  M9    │  M10   │  M11   │
│  环境  │  框架  │  离线  │  MVP   │  Web   │  Web   │  Web   │  功耗  │  体验  │  功能  │  测试  │  发布  │
│  搭建  │  搭建  │  复习  │  完成  │  服务  │  管理  │  完整  │  优化  │  优化  │  补全  │  修复  │  上线  │
├────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┼────────┤
│◄──── Phase 0 ────►│◄────────────── Phase 1 ──────────────►│◄────────────── Phase 2 ──────────────►│
│                    │                                       │                                       │
│                    │◄──────────────── Phase 3 ────────────────────────────────►│◄── Phase 4 ──►│
└────────────────────┴───────────────────────────────────────────────────────────┴───────────────┘
```

---

### 11.8 风险与缓解

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| IT8951驱动不稳定 | 显示异常 | 中 | Phase 0充分验证，准备备选驱动库 |
| APKG格式兼容性复杂 | 导入失败率高 | 高 | 先支持基础格式，逐步扩展 |
| FSRS浮点运算性能不足 | 计算慢 | 低 | ESP32-S3有硬件FPU，备选定点运算 |
| WiFi功耗超标 | 续航不足 | 中 | 严格定时策略，Phase 3专项优化 |
| Flash空间不足 | 功能裁剪 | 中 | Web资源极致压缩，SD卡分流 |
| E-Ink刷新体验差 | 用户不满意 | 中 | Phase 3专项优化波形和刷新策略 |
| SQLite数据损坏 | 用户数据丢失 | 低 | WAL模式+定期检查+自动备份 |

---

## 12. 版本迭代规划

- **V1.0（当前，Phase 0~4 交付）**：离线复习 MVP + Web 管理后台 + FSRS V5；目标：独立可用、核心体验达标。交付时间：Week 12。
- **V1.1（功能补全，Week 13~14）**：AnkiConnect 协议桥接、多语言 i18n 框架、复习日志 CSV 导出、媒体文件预览/清理。目标：补全 M9 系列功能并打磨交互细节。
- **V1.5（同步生态，Week 15~18）**：AnkiWeb 双向同步（手动触发）、AnkiConnect WiFi 同步、多牌组学习计划、LaTeX 公式离线渲染（MathJax 精简版）。目标：打通 PC/手机与硬件终端的复习流。
- **V2.0（云+AI，远期，Week 19+）**：云端备份归档、AI 辅助卡片生成（LLM API）、多设备协同复习进度、开放插件扩展机制。目标：从专用硬件升级为记忆学习平台。

---

## 附录

### A. 关键参考文档
- FSRS V5 算法论文: https://github.com/open-spaced-repetition/fsrs4anki。
- M5Stack PaperS3 文档: https://docs.m5stack.com/en/core/PaperS3。
- IT8951 驱动数据手册。
- AXP2101 PMU 数据手册。
- Anki APKG 文件格式: https://github.com/ankidroid/Anki-Android/wiki/Database-Structure。

### B. 开源依赖清单
- ESP-IDF v5.1+。
- M5Unified / M5EPD 库。
- esp32-sqlite3。
- miniz (ZIP解析)。
- cJSON (JSON处理)。
- 前端: Tailwind CSS + Chart.js (精简版)。
- 参考：PortableAnki (ESP32 Anki 硬件参考实现)：https://github.com/zykkkl/PortableAnki。
- 参考：crosspoint-reader-papers3-fork (ESP32 电子书 App 框架、EPD 驱动、CJK 字体移植)：https://github.com/byronwind/crosspoint-reader-papers3。

### C. 固件构建命令参考
```bash
# 克隆仓库
git clone https://github.com/your-org/AnkiEInk-FW.git
cd AnkiEInk-FW

# 设置ESP-IDF环境
. ~/esp/esp-idf/export.sh

# 设置目标芯片
idf.py set-target esp32s3

# 编译固件
idf.py build

# 烧录到设备
idf.py -p /dev/ttyUSB0 flash

# 监控串口输出
idf.py -p /dev/ttyUSB0 monitor
```


---

**文档结束**

*本文档为AnkiEInk for M5Stack PaperS3完整产品需求文档，版本V1.1（修订版）。所有开发工作应基于此文档进行，需求变更需通过正式变更流程。*